# 修仙桌宠（Tomato）项目说明文档

> 一款基于 **Godot 4.4** 的桌面修仙陪伴 Widget：透明窗口、无边框、总在最前的纯净桌面精灵，集成番茄钟与修仙打卡养成系统。

---

## 一、项目定位

| 维度 | 说明 |
|---|---|
| 类型 | 桌面伴随型 Widget（番茄钟 + 养成） |
| 引擎 | Godot 4.4（Forward Plus） |
| 语言 | GDScript |
| 平台 | Windows Desktop（已配置导出预设） |
| 窗口形态 | `borderless=true` + `transparent=true` + `always_on_top=true` |
| 存档 | `user://save_data.json`（本地 JSON） |
| 主题包装 | 修仙世界观：境界 / 闭关 / 走火入魔 / 罡气 |

---

## 二、目录结构总览

```
tomato/
├── project.godot              # Godot 工程配置（窗口/自动加载/输入映射）
├── export_presets.cfg         # Windows Desktop 导出预设
├── .gitignore                 # 忽略 .godot、godot_engine、build、源图等
├── 方案.md                    # 原始需求与里程碑文档
│
├── run.bat                    # 双击启动（调用 godot_engine 中的可执行）
├── build_exe.bat              # 一键导出 build/修仙桌宠.exe
├── generate_sprites.py        # Python 像素素材生成脚本（基于 Pillow）
│
├── scenes/
│   └── main.tscn              # 主场景（Node2D + UIPanel）
│
├── scripts/                   # 全部 GDScript 源码
│   ├── main.gd                # 主控（窗口拖拽 / 子系统装配 / 信号路由）
│   ├── state_machine.gd       # 三态状态机（IDLE/FOCUS/SLEEP）
│   ├── anim_controller.gd     # 状态 → 动画名映射播放
│   ├── pomodoro.gd            # 番茄钟（Timer 封装）
│   ├── save_manager.gd        # ★ Autoload 单例：存档 + 经验 + 境界
│   ├── ui_panel.gd            # 修仙交互面板（按钮/状态/计时显示）
│   └── sprite_generator.gd    # 代码绘制像素小人（无素材时 fallback）
│
└── assets/sprites/            # 三组动画帧（PNG，96x96）
    ├── idle/   01-06.png      # 待机：呼吸悬浮
    ├── focus/  01-08.png      # 闭关：打坐吐纳
    └── sleep/  01-06.png      # 走火入魔：歪头冒泡
```

> 运行时若 `assets/sprites/idle/` 中存在 PNG，会优先加载自定义素材；否则由 [SpriteGenerator](file:///d:/Desktop/tomato/scripts/sprite_generator.gd) 即时生成代码绘制的像素小人。

---

## 三、核心配置

### 3.1 [project.godot](file:///d:/Desktop/tomato/project.godot)

| 配置项 | 值 | 作用 |
|---|---|---|
| `application/run/main_scene` | `res://scenes/main.tscn` | 入口场景 |
| `autoload.SaveManager` | `*res://scripts/save_manager.gd` | 全局存档单例 |
| `display/window/size` | 480 × 280 | 视窗尺寸 |
| `borderless` / `always_on_top` / `transparent` | `true` | 桌宠三要素 |
| `per_pixel_transparency/allowed` | `true` | 允许逐像素透明 |
| `rendering/viewport/transparent_background` | `true` | 视口透明 |
| `input.right_click` | `MOUSE_BUTTON_RIGHT` | 右键开关面板（动作映射） |

### 3.2 [export_presets.cfg](file:///d:/Desktop/tomato/export_presets.cfg)

- 平台：Windows Desktop
- 输出：`build/修仙桌宠.exe`
- `binary_format/embed_pck=true`：单 EXE 直出（PCK 内嵌）
- 产品名：`修仙桌宠`，描述：`修仙桌面宠物 - 番茄钟`

---

## 四、场景结构 [main.tscn](file:///d:/Desktop/tomato/scenes/main.tscn)

```
Main (Node2D, script = main.gd)
└── UIPanel (PanelContainer, script = ui_panel.gd, z_index=10)
    └─ 偏移 (220, 10) ~ (470, 270)，位于窗口右半区
```

> 角色相关节点（`Sprite2D 光晕`、`Area2D 碰撞`、`AnimatedSprite2D 主体`、`CPUParticles2D 灵气粒子`、`Label 状态标签`）**全部在 `main.gd::_build_scene()` 中代码生成**，未在 .tscn 中显式定义。

---

## 五、脚本职责详解

### 5.1 [main.gd](file:///d:/Desktop/tomato/scripts/main.gd) —— 主控制器
**继承**：`Node2D` | **行数**：472

负责装配所有子系统、连接信号、处理窗口级输入。

**核心字段**

| 字段 | 类型 | 用途 |
|---|---|---|
| `state_machine` | `CharacterStateMachine` | 三态切换 |
| `anim_controller` | `CharacterAnimController` | 状态→动画 |
| `pomodoro` | `PomodoroTimer` | 番茄计时 |
| `ui_panel` | `GameUIPanel` | 交互面板 |
| `is_dragging` / `drag_offset` | `bool` / `Vector2i` | 窗口拖拽 |
| `_can_move_window` | `bool` | 嵌入模式探测 |
| `is_at_edge` / `edge_side` | `bool` / `String` | 屏幕贴边状态 |

**关键流程**

1. `_ready()`：
   - 居中窗口 → `_build_scene()`（光晕/Area2D/精灵/粒子）→ 装配三大子系统
   - 加载动画帧（`_dir_has_images` 优先用 `assets/sprites/`，否则 `SpriteGenerator.generate_all_frames()`）
   - 通过 `Image.load()` + `globalize_path` **绕过 Godot import 系统**直接读取 PNG（避免 `.import` 依赖）
   - 连接所有跨模块信号（见第六节）

2. `_process()`：
   - 拖拽时：`DisplayServer.window_set_position(mouse - drag_offset)`，并 `_clamp_to_screen` 防止飞出
   - 番茄进行中：实时刷新 UI 计时显示
   - `_check_screen_edge()` 检测窗口贴边

3. **贴边动画**：`_enter_edge_mode(side)` 根据方向给精灵加旋转 + 挤压形变（左右倾斜 25°、上方倒立、下方压扁）

4. **信号回调**：
   - `_on_start_focus / _on_interrupt_focus` → 控制番茄钟 + 状态机 + UI
   - `_on_focus_completed` → `SaveManager.focus_completed()`，回到 IDLE
   - `_on_state_changed` → 切换粒子颜色/数量、光晕色温
   - `_on_task_completed` → 调用 `SaveManager.complete_task()` 累计经验

---

### 5.2 [state_machine.gd](file:///d:/Desktop/tomato/scripts/state_machine.gd) —— 状态机
**class_name**：`CharacterStateMachine` | **信号**：`state_changed(new_state: String)`

```
enum State { IDLE, FOCUS, SLEEP }
```

| 方法 | 含义 |
|---|---|
| `go_idle()` | 回到待机 |
| `go_focus()` | 进入闭关（番茄启动） |
| `go_sleep()` | 走火入魔（番茄中断） |
| `change_state()` | 状态去重 + 发射信号 |
| `get_state_name()` | 返回字符串名（用于 UI/动画键） |

---

### 5.3 [anim_controller.gd](file:///d:/Desktop/tomato/scripts/anim_controller.gd) —— 动画控制
**class_name**：`CharacterAnimController`

极简映射器：把状态名小写后作为动画键去 `SpriteFrames` 查找并 `play()`。

```gdscript
play_animation("FOCUS") → animated_sprite.play("focus")
```

---

### 5.4 [pomodoro.gd](file:///d:/Desktop/tomato/scripts/pomodoro.gd) —— 番茄钟
**class_name**：`PomodoroTimer`
**信号**：`focus_started` / `focus_completed` / `focus_interrupted` / `timer_tick`

- 默认时长：`25 * 60` 秒（可由 `start_focus(duration)` 覆盖）
- 内部用 `Timer(one_shot=true)` 单发倒计时
- `interrupt()` 立即停表 + 发 `focus_interrupted`，**不扣经验**
- 自然到时 `_on_timeout()` → `focus_completed` 信号
- `get_time_left_string()` 返回 `MM:SS` 字符串供 UI 直接使用

---

### 5.5 [save_manager.gd](file:///d:/Desktop/tomato/scripts/save_manager.gd) —— 全局存档单例 ★
**class_name**：`SaveManagerSingleton` | **Autoload**：`SaveManager`
**信号**：`data_changed` / `realm_changed(new_realm)`

**境界系统**

| Index | 境界 | 经验阈值 |
|---|---|---|
| 0 | 练气 | 0 |
| 1 | 筑基 | 100 |
| 2 | 金丹 | 300 |
| 3 | 元婴 | 600 |
| 4 | 化神 | 1000 |
| 5 | 炼虚 | 1500 |
| 6 | 合体 | 2200 |
| 7 | 大乘 | 3000 |
| 8 | 渡劫 | 4000 |

**默认数据结构**（[get_default_data](file:///d:/Desktop/tomato/scripts/save_manager.gd#L18-L32)）

```json
{
  "realm_index": 0,
  "realm": "练气",
  "total_exp": 0,
  "body_strength": 0,         // 上限 100
  "focus_count": 0,
  "tasks_today": { "gongfa": false, "zhenfa": false, "like": false, "routi": false },
  "last_save": ""
}
```

**核心 API**

| 方法 | 行为 |
|---|---|
| `load_data()` | 读取 `user://save_data.json`，缺键自动用默认值补齐 |
| `save_data()` | JSON.stringify(缩进2空格) 持久化 + 写入 `last_save` 时间戳 |
| `add_exp(amount)` | 累加经验 → 检查升境 → 存盘 → 发 `data_changed` |
| `add_body_strength(amount)` | 累加罡气（max 100）→ 存盘 |
| `complete_task(key, exp, is_body)` | 当日已完成则返回 false；否则置 true 并发经验 |
| `focus_completed()` | `focus_count++` 且 `add_exp(25)` |
| `_check_realm_upgrade()` | 从高到低扫描阈值，命中即发 `realm_changed` |
| `get_exp_to_next_realm()` | UI 进度提示用 |
| `reset_daily_tasks()` | 每日重置打卡（**当前未自动调用**，预留） |

---

### 5.6 [ui_panel.gd](file:///d:/Desktop/tomato/scripts/ui_panel.gd) —— 修仙交互面板
**class_name**：`GameUIPanel` | **继承**：`Control`
**信号**：`start_focus_pressed` / `interrupt_focus_pressed` / `task_completed(key, exp, is_body)`

**UI 组成**（全部代码生成 StyleBoxFlat 配色，无 Theme 资源）

```
PanelContainer（深蓝半透明 + 圆角边框）
└── VBoxContainer
    ├── 标题  "修仙面板"
    ├── 状态行 "境界: X | EXP: Y (还差Z)\n罡气: A/100"
    ├── 番茄区
    │   ├── 大号倒计时 "25:00"
    │   └── [开始闭关 / 中断闭关] 按钮（绿色）
    ├── 日常修炼（4 个任务按钮，紫色）
    │   ├── gongfa  功法修炼 (CET-4)  +15 EXP
    │   ├── zhenfa  阵法推演 (编程)   +20 EXP
    │   ├── like    理科悟性 (数学)   +15 EXP
    │   └── routi   肉体横练 (运动)   +30 罡气   ← is_body=true
    ├── 消息提示  message_label（黄色，倒计时清空）
    └── 操作提示  "右键: 开关面板 | 拖拽: 移动"
```

**对外接口**

| 方法 | 用途 |
|---|---|
| `update_status(realm, exp, body, next_exp)` | 刷新状态行 |
| `update_timer_display(str)` / `reset_timer_display()` | 计时显示 |
| `set_focus_active(active)` | 切换闭关按钮文字 |
| `disable_task(key)` | 完成后置灰按钮并加 "[已完成]" |
| `show_message(text, duration=3.0)` | 短暂提示，`await` 自动清除 |

> **可见性控制**：默认 `visible=false`，由 `main.gd::_input()` 监听**全局右键**触发 `_toggle_ui_panel()`。

---

### 5.7 [sprite_generator.gd](file:///d:/Desktop/tomato/scripts/sprite_generator.gd) —— 代码绘制 fallback
**class_name**：`SpriteGenerator` | **继承**：`RefCounted`（静态工具类）

无需任何美术资源，纯算法绘制 96×96 像素小人三组动画：

| 动画 | 帧数 | 速度 | 内容 |
|---|---|---|---|
| `idle` | 8 | 8 fps | 站立小道士，呼吸上下浮动、衣袂随 sin 摆动、刘海发髻、靴子腰带 |
| `focus` | 6 | 6 fps | 盘坐蒲团，外环灵气脉冲、经脉流光（5 个光点流动） |
| `sleep` | 10 | 5 fps | 盘坐歪头、闭眼、嘴角鼻涕泡渐变、ZZZ 浮动文字 |

**绘图工具**：`_fill_circle / _fill_ellipse / _draw_rect / _draw_line_thick / _draw_z_letter / _blend_pixel`（手动 alpha 合成）。

---

## 六、信号通信图

```
[UIPanel]                         [Main]                      [Pomodoro]
   │ start_focus_pressed  ───────► _on_start_focus  ─────────► start_focus()
   │ interrupt_focus_pressed ────► _on_interrupt_focus  ─────► interrupt()
   │ task_completed(k,e,b)  ─────► _on_task_completed
                                          │
                                          ▼
                                   [SaveManager]
                                     complete_task()
                                     focus_completed()
                                          │
                                          ├─ data_changed   ──► _on_data_changed → UI 状态刷新
                                          └─ realm_changed  ──► 弹突破提示

[Pomodoro]
   │ focus_completed   ──────────► _on_focus_completed → SaveManager.focus_completed()
   │ focus_interrupted ──────────► _on_focus_interrupted

[StateMachine]
   │ state_changed(name)  ───────► _on_state_changed
                                          ├─ AnimController.play_animation()
                                          └─ 调整粒子颜色 / 光晕色温
```

---

## 七、运行 & 打包

### 7.1 运行（开发态）
```bat
:: 需先把 Godot_v4.4-stable_win64_console.exe 放到 godot_engine\ 目录
run.bat
```

### 7.2 打包 EXE
```bat
:: 输出 build\修仙桌宠.exe（embed_pck=true 单文件）
build_exe.bat
```

### 7.3 重新生成像素素材（可选）
```bat
:: 需要 Pillow：pip install pillow
:: 准备 source_character.jpg 后运行：
python generate_sprites.py
```
将基于源图自动产出 idle / focus / sleep 三组动画帧到 `assets/sprites/`。

---

## 八、交互速查

| 操作 | 行为 |
|---|---|
| **左键拖拽精灵** | 移动整个桌宠窗口（基于 `DisplayServer.window_set_position`） |
| **右键任意位置** | 开 / 关 修仙面板 |
| **窗口贴屏幕边** | 精灵自动倾斜 / 挤压（左右斜 25°、上倒立、下压扁） |
| **开始闭关** | 25 分钟倒计时，状态切 FOCUS，灵气粒子加倍 |
| **中断闭关** | 切 SLEEP（走火入魔），不扣经验 |
| **闭关完成** | +25 EXP，回到 IDLE |
| **打卡日常** | 累加 EXP / 罡气，按钮置灰 |

---

## 九、与 [方案.md](file:///d:/Desktop/tomato/方案.md) 里程碑对照

| 里程碑 | 状态 | 落地位置 |
|---|---|---|
| M1 透明无边框 + 拖拽 | ✅ | [project.godot](file:///d:/Desktop/tomato/project.godot) `[display]` + [main.gd](file:///d:/Desktop/tomato/scripts/main.gd) `_process` |
| M2 状态机 + 占位素材 | ✅ | [state_machine.gd](file:///d:/Desktop/tomato/scripts/state_machine.gd) + [sprite_generator.gd](file:///d:/Desktop/tomato/scripts/sprite_generator.gd) |
| M3 番茄钟 + 闭环 | ✅ | [pomodoro.gd](file:///d:/Desktop/tomato/scripts/pomodoro.gd) + [ui_panel.gd](file:///d:/Desktop/tomato/scripts/ui_panel.gd) |
| M4 JSON 存档 + 打卡 | ✅ | [save_manager.gd](file:///d:/Desktop/tomato/scripts/save_manager.gd) |

---

## 十、可扩展点（已留接口未实现）

1. **每日打卡重置**：`SaveManager.reset_daily_tasks()` 已有，需在启动时比对 `last_save` 日期触发。
2. **粒子升级**：`FOCUS` 状态目前用 `CPUParticles2D`，可换 `GPUParticles2D` + 自定义 ParticleProcessMaterial。
3. **境界突破特效**：`realm_changed` 仅弹文本，可叠加全屏闪光 / 音效。
4. **多窗口嵌入兼容**：`_can_move_window` 已具备探测，但贴边逻辑在嵌入模式下被禁用。
5. **音频**：当前无 AudioStreamPlayer；可在 focus_completed / realm_changed 触发音效。
