# 修仙桌宠 · 优化需求文档

> 版本：v1.0（评审通过 · 本轮基线）
> 日期：2026-05-29
> 范围：基于当前已交付版本（透明窗口 + 番茄钟 + 三态状态机 + 境界系统 + 自定义任务下拉 + 多形象切换）的下一轮迭代
> **本轮锁定范围**：R-01 / R-02 / R-06（详见第四章评审决议）
> **通用约束**：所有动 `SaveManager.data` 结构的改动必须写「读旧存档自动迁移」逻辑，禁止清档

---

## 0. 文档使用说明

- 每条需求按 **背景 → 需求 → 验收标准 → 技术要点 → 优先级** 五段式描述
- 优先级：⭐⭐⭐ 必须做 / ⭐⭐ 建议做 / ⭐ 锦上添花
- 实施前需逐项与用户确认是否纳入本轮迭代

---

## 一、高频体验痛点

### R-01 自动每日重置 ⭐⭐⭐

- **背景**：[`SaveManager.reset_daily_tasks()`](file:///d:/Desktop/tomato/scripts/save_manager.gd) 函数已存在但**从未被调用**，跨天后 `tasks_done_today` 不会清空，打卡按钮仍是灰态——这是 bug 级别遗留。
- **需求**：
  1. 启动时对比 `last_save` 日期，跨天则自动调用 `reset_daily_tasks()`
  2. 重置前把昨日完成数写入 `history: {date: completed_count}`
  3. 新开一天首次打开桌宠，弹出气泡「道友醒来，新的一日修炼开始」
- **验收**：
  - 改本地系统时间 +1 天，重启程序后所有任务恢复可打卡
  - `SaveManager.data.history` 正确累积昨日数据
- **技术要点**：在 [`main.gd`](file:///d:/Desktop/tomato/scripts/main.gd) `_ready()` 内插入 `_check_daily_rollover()` 逻辑
- **优先级**：⭐⭐⭐

### R-02 番茄钟时长可配置 + 休息提醒 ⭐⭐⭐

- **背景**：[`pomodoro.gd`](file:///d:/Desktop/tomato/scripts/pomodoro.gd) 把 25 分钟写死，结束直接回 idle，没有"该休息了"提示，对长短番茄派都不友好。
- **需求**：
  1. 设置面板新增三个数字输入：**专注时长**（默认 25）/ **短休息**（默认 5）/ **长休息**（默认 15）
  2. 每完成 4 个番茄触发长休息一次
  3. 休息阶段桌宠切换为「打坐回气」动画 + 倒计时浮窗
  4. 休息结束自动回 idle，弹出「闭关已毕，可继续修炼」
- **验收**：
  - 设置改为 1/1/2 分钟可在 5 分钟内验证完整循环
  - 第 4 个番茄结束时进入长休息而非短休息
- **技术要点**：
  - `pomodoro.gd` 增加 `mode: focus/short_break/long_break` 状态
  - `SaveManager.data` 增加 `settings: {focus_min, short_break_min, long_break_min}`
  - 新增 `休息` 资源动画（可暂用 sleep 帧复用）
- **优先级**：⭐⭐⭐

### R-03 任务周期 + 经验值边界保护 ⭐⭐

- **背景**：当前所有任务都是「每日打卡」，无法表达「每周 3 次」「一次性挑战」；经验输入框无校验，可输入 99999 破坏数值。
- **需求**：
  1. `task_def` 新增 `cycle: "daily" | "weekly" | "once"` 字段
  2. 下拉列表按图标分组：☀️日常 / 📅每周 / 🎯一次性
  3. 新增任务面板加 `cycle` 选择框
  4. 经验值输入框 clamp 到 `[1, 200]`，越界提示
  5. 周期 `weekly` 在每周一重置；`once` 完成后自动从列表删除
- **验收**：
  - 新建一个 `weekly` 任务，跨过周一后恢复可打卡
  - 输入 -5 / 9999 / 空 都不会破坏数据
- **优先级**：⭐⭐

### R-04 切换形象改为下拉 ⭐⭐

- **背景**：当前是循环按钮，3 个以上形象不直观，且无法直接跳到指定形象。
- **需求**：把循环按钮换成 `OptionButton`，列出 `available_characters`，选中即切；保留键盘快捷键（可选）
- **验收**：3 个以上形象可任意跳转
- **技术要点**：复用 R-03 中的 OptionButton 经验，[`ui_panel.gd`](file:///d:/Desktop/tomato/scripts/ui_panel.gd) 改约 30 行
- **优先级**：⭐⭐

---

## 二、修仙主题沉浸感

### R-05 数据统计 + 成就系统 ⭐⭐

- **背景**：用户长期使用没有可视化反馈，难以坚持。
- **需求**：
  1. **每日热力图**：仿 GitHub，显示近 30/90 天番茄完成数（颜色深浅）
  2. **境界进度条**：当前经验 / 下一境界所需，配境界图标
  3. **道心稳固**：连续打卡天数（任意一项任务即可计数）
  4. **成就墙**（基础 8 条）：
     - 初入修仙（首次完成番茄）
     - 道心初坚（连续 7 天）
     - 道心通玄（连续 30 天）
     - 一日筑基（单日 8 个番茄）
     - 首次破境
     - 走火入魔（首次中断专注）
     - 闭关三日（连续 3 天每天 ≥4 番茄）
     - 收集癖（解锁全部内置形象）
- **验收**：完成对应条件后弹出成就气泡，存档持久化
- **技术要点**：新建 `scripts/achievement.gd` 单例 + `scenes/stats_panel.tscn`
- **优先级**：⭐⭐

### R-06 境界突破仪式感 ⭐⭐⭐

- **背景**：当前经验涨够直接 +1，毫无视觉反馈，修仙主题最大卖点反而最寡淡。
- **需求**：
  1. 突破时**全屏闪光**（白色 fade in/out 0.6s）
  2. 桌宠播放 **break_through 动画**（金光环 + 上升）
  3. 飘字「**恭喜道友突破至 XX 境**」（金色描边，3 秒淡出）
  4. **金丹期及以上需渡劫**：进度满后进入「渡劫态」，必须连续完成 1 个番茄才能真正晋升；中途中断 → 走火入魔，**保留当前阶位、当前阶位经验清零**（不降阶，温和惩罚）
  5. 渡劫期间桌宠为特殊 `tribulation` 动画（雷电 / 头顶乌云）
- **验收**：练气满 → 筑基有完整闪光与飘字；金丹满进入渡劫态，模拟成功 / 失败两种结局
- **技术要点**：
  - [`state_machine.gd`](file:///d:/Desktop/tomato/scripts/state_machine.gd) 新增 `BREAKTHROUGH` / `TRIBULATION` 状态
  - 飘字层用 `CanvasLayer` + `Label` + `Tween`
- **优先级**：⭐⭐⭐

### R-07 桌宠点击交互（台词气泡 + 彩蛋） ⭐

- **背景**：桌宠目前是个静态吉祥物，缺乏陪伴感。
- **需求**：
  1. **双击桌宠**：触发台词气泡，按当前境界 + 状态从 JSON 库随机抽一句
     - 练气期 idle："道友，今日修炼了吗？"
     - 渡劫期 focus："专心些，莫让心魔趁虚而入"
     - sleep："Zzz... 入定中"
  2. **5 秒内连点 5 次**：彩蛋——桌宠跳一段抖头动画 + "再点贫道发火了！"
  3. 台词库放 `assets/dialogues.json`，便于扩展
- **验收**：不同境界 / 状态台词不重叠；连点彩蛋触发后冷却 30 秒
- **优先级**：⭐

### R-08 丹药 / Buff 系统 ⭐

- **背景**：长期使用缺乏奖励循环。
- **需求**：
  1. 累计专注时长达阈值随机掉落丹药
  2. 初期 2~3 种：
     - **凝神丹**（下一番茄经验 ×1.5）
     - **破障丹**（境界进度 +20%）
     - **辟谷丹**（跳过下一次休息）
  3. 背包 UI（简单列表，点击使用）
- **验收**：专注完成后概率（10%）掉落，使用后效果生效
- **优先级**：⭐

---

## 三、工程健壮性 / 实用性

### R-09 系统托盘 + 开机自启 + 全局热键 ❌ 移出范围

- **决议**：托盘 / 自启 / 全局热键均超出 Godot 原生能力，需 GDExtension 或外挂守护进程，复杂度高、维护成本大，**本项目不做**
- **保留可行替代**：用户可手动把 `run.bat` 的快捷方式放到 `shell:startup` 实现开机自启，无需代码
- **优先级**：❌ 不做

### R-10 存档导入导出 + 自动备份 ⭐⭐

- **需求**：
  1. 设置面板加「**导出存档** / **导入存档** / **重置**」三按钮
  2. 每日启动自动备份到 `user://backups/YYYY-MM-DD.json`，保留最近 7 天
  3. 重置需二次确认
- **验收**：导出 → 删除 → 导入，数据完整恢复
- **优先级**：⭐⭐

### R-11 收尾杂项 ⭐

- `main.tscn` UID 警告（`invalid UID: uid://3gcvsmvgdu3x`）：编辑器重存一次清除
- [`run.bat`](file:///d:/Desktop/tomato/run.bat) 增加 `chcp 65001` 修复控制台中文乱码
- 控制台日志加时间戳前缀，方便排查
- **优先级**：⭐

### R-12 `make_character.py` 升级 ⭐

- **需求**：
  1. 支持 `--state idle/focus/sleep` 单独替换某状态帧
  2. 输出预览大图（4×N 拼接）到 `assets/characters/<name>/preview.png`
  3. 可选：`tkinter` GUI 化（拖图 + 实时预览）
- **优先级**：⭐

---

## 四、实施路线图（建议）

### 第 1 轮（本次）—— 基础修复 + 仪式感
> 目标：补 bug + 把"修仙感"立起来

- [ ] R-01 自动每日重置
- [ ] R-02 番茄钟可配置 + 休息提醒
- [ ] R-06 境界突破仪式感

### 第 2 轮 —— 易用性 + 数据
- [ ] R-03 任务周期 + 边界保护
- [ ] R-04 形象切换下拉
- [ ] R-05 数据统计 + 成就

### 第 3 轮 —— 沉浸感深化
- [ ] R-07 点击交互台词
- [ ] R-08 丹药系统
- [ ] R-10 存档备份

### 第 4 轮 —— 实用性 / 收尾
- [ ] R-09 托盘 / 自启 / 热键
- [ ] R-11 杂项收尾
- [ ] R-12 make_character 升级

---

# 五、评审决议（2026-05-29 已确认）

| 议题 | 决议 |
|---|---|
| 1. 本轮迭代范围 | **锁定 R-01 / R-02 / R-06 三项**，其余项推迟到后续轮次 |
| 2. R-06 渡劫失败惩罚 | **保留阶位 + 当前阶位经验清零**（温和方案） |
| 3. R-09 托盘 / 自启 / 热键 | **移出范围，不做**；用户可手动放快捷方式到启动文件夹 |
| 4. 存档迁移 | **必须做**；R-01/R-02 改 `SaveManager.data` 结构时，写"读旧存档自动迁移"逻辑 |
| 5. 遗漏项 | 无 |

---

> 本文档已视为本轮迭代的基线契约；任何范围变更需更新此文档对应章节。
