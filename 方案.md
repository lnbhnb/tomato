# 核心目标 (Project Overview)
自主构建一款基于 Godot 4 的桌面修仙陪伴类 Widget（全自动建项目、写代码、调试）。
游戏运行形态必须是：**无边框 (Borderless)、背景透明 (Transparent)、总在最前 (Always on top)** 的纯净桌面精灵。

# 技术栈与规范 (Tech Stack & Specs)
- **引擎**: Godot 4.x
- **语言**: GDScript
- **存储**: 本地 JSON 文件读写 (`user://save_data.json`)
- **架构**: 采用基于 Godot 节点树的组件化设计，核心逻辑通过 Signal（信号）解耦。
- **UI**: 使用 Control 节点构建极简交互面板，平时隐藏，鼠标交互时呼出。

# 核心模块拆解 (Core Modules)

## 1. 视窗与输入管理 (Window & Input Manager)
- **要求**: 
  - 通过 `project.godot` 配置或 `DisplayServer` API 强制实现窗口全透明和无边框。
  - 根节点挂载拖拽逻辑：监听 `InputEventMouseButton`，按住精灵的有效碰撞区（Area2D）时，通过 `DisplayServer.window_set_position` 改变操作系统窗口位置。

## 2. 状态机与动画系统 (State Machine & Animation)
- **要求**: 精灵拥有三种基础状态，通过简单的状态机脚本管理。
  - `IDLE` (待机): 播放基础呼吸/悬浮动画。
  - `FOCUS` (闭关): 播放打坐吐纳动画（预留粒子特效接口）。
  - `SLEEP` (走火入魔/打盹): 播放冒鼻涕泡动画。

## 3. 番茄钟核心 (Pomodoro Core)
- **要求**: 
  - 封装一个 Timer 组件。启动时设定倒计时（默认 25 分钟），状态切为 `FOCUS`。
  - 支持中途“中断”操作，中断后状态切为 `SLEEP`，不扣除存量经验。
  - 倒计时自然结束 (`timeout`)，触发 `focus_completed` 信号。

## 4. 数据持久化与任务系统 (Save & Task System)
- **要求**:
  - 创建单例 (Autoload) `SaveManager`。
  - 数据结构需包含玩家境界、总经验值 (EXP)、肉身罡气值等。
  - UI 预留日常打卡面板，包含但不限于以下类别（点击后增加对应经验并存入 JSON）：
    - 功法修炼（如：CET-4 真题模拟，EXP +15）
    - 阵法推演（如：Spark/Python 数据清洗脚本完成，EXP +20）
    - 理科悟性（如：微积分导数练习，EXP +15）
    - 肉体横练（如：完成 1000米跑/引体向上，肉身经验 +30）

# 自动执行里程碑 (Execution Milestones)
请 Agent 按照以下顺序逐步完成开发与测试：
1. **Milestone 1**: 创建 Godot 项目，修改引擎配置实现透明/无边框，并完成拖拽逻辑。
2. **Milestone 2**: 搭建基础场景树，导入占位像素素材，编写状态机切换逻辑。
3. **Milestone 3**: 实现 Timer 计时器与 UI 按钮交互，跑通“开始专注-中断/完成”的闭环。
4. **Milestone 4**: 完成 JSON 存档系统的序列化与反序列化，实现打卡任务的数据累加。